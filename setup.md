# Setup

- In this folder, create a NextJS project with Typescript and React using yarn according to https://nextjs.org/docs/app/getting-started/installation
- The dev-server should run with HTTPS to allow some integration with external APIs
- The SSL certificate should be created and the values needed for the SSL dev-server should be added as environment variables
  - For local development, add these environment variables to the .env.local file